namespace IritNet
{
    public unsafe struct UserGCObsPtTypeStruct
    {
        public double ZAngle;                      /* Angle in degrees in the Z axis. */
        public double XYAngle;                   /* Angle in degrees in the XY plane. */
    }
}
